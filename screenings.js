export function createScreenings(app, movieId) {
  sideStart(app);
}

const sideStart = (app) => {
  app.innerHTML = `
         <h2>kom nu peter</h2>
         <p>du kan sikkert ikke finde ud af spille dh.</p>
         <p>skal du moses</p>
     `;
};
